# Conventions

House conventions for the workspace: cite sources, never invent file paths, keep
the router thin, and route detail into the homes rather than inlining it here.
This companion is structural and shared; it carries no personal instance content,
so it projects verbatim.
