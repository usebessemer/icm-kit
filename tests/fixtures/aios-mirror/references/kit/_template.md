# Kit template

A reference catalog template. Named in the `catalog` load row as a bare
`_template.md` qualified by the same-cell `references/kit/` directory token, so
F3 must resolve it (not flag it as missing).
