# L1 to L1

Lateral messages between role workspaces: one thread handing another something it needs, without routing through the root. Append at the end, dated, naming both the sending and receiving threads.

## Messages

_(empty; append cross-thread messages below)_
