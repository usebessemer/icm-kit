# Sync log

A running record of cross-machine syncs: when the workspace was last reconciled, from where, and anything that needed manual resolution. It makes a missed sync visible rather than silent. The protocol this serves is [`../sync/protocol.md`](../sync/protocol.md).

## Log

_(empty; append a dated line per sync)_
