# Thread registry

The index of every thread the workspace tracks: one row per thread, pointing at its workspace under [`workspaces/`](workspaces/). The state board says what is live; this registry says what exists at all.

## Threads

_(threads populate from discovery)_

| Thread | Workspace | Status | Notes |
|---|---|---|---|
| _(none yet)_ | | | |

A thread earns a row here when it is stood up as a role workspace. Adding one follows [`EXPANSIONS.md`](EXPANSIONS.md); retiring one moves its row's detail into [`decisions/log.md`](decisions/log.md) rather than deleting the history.
